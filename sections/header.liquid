<header>
    <h1>{{ section.settings.title }}</h1>
    <img src="{{ section.settings.image | img_url: 'large' }}" alt="">
    <div>
        {{ canonical_url }}
        {% assign today_date = 'now' | date: '%s' %}
        <p>Today timestamp: {{ today_date }}</p>
        {% for product in collections['creams'].products %}
            {% assign created_at = product.created_at | date: '%s' %}
            <p>{{ product.title }} created at {{ created_at | date: '%s' }}</p>
            {% if today_date < created_at %}
                <p>Bazinga, hoy es mas viejo </p>
            {% else %}
                <p>Hoy es mas nuevo</p>
            {% endif %}
        {% endfor %}
    </div>
</header>

{% schema %}
{
    "name": "header",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "test_section"
        },
        {
           "type": "image_picker",
           "id": "image",
           "label": "Header image"
        }
    ]
}
{% endschema %}
